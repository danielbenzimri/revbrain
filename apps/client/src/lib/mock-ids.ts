/**
 * Client-side Mock IDs
 *
 * Re-exported from @revbrain/seed-data (single source of truth).
 * The parity test at apps/server/src/mocks/mock-ids-parity.test.ts
 * verifies these match the server-side constants.
 */
export { MOCK_IDS } from '@revbrain/seed-data';
