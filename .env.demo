# --- RevBrain -- Demo Mode ------------------------------------------------
# External demo for partner reviews at demo.revbrain.ai.
# Mock data, no auth, no real resources. Safe to share publicly.
#
# This file is COMMITTED to the repo. It contains zero secrets.

NODE_ENV=production
APP_MODE=demo
USE_MOCK_DATA=true
AUTH_MODE=mock
VITE_AUTH_MODE=mock
VITE_DEMO_MODE=true
VITE_API_URL=https://qutuivleheybnkbhpdbn.supabase.co/functions/v1/demo-api
