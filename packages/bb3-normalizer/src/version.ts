/**
 * Single source of truth for BB-3's version string. Imported by
 * the pipeline and stamped on `NormalizeResult.runtimeStats.bb3Version`.
 * Keep in sync with package.json.
 */
export const BB3_VERSION = '0.0.0-ph3';
